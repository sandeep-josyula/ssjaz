on:
  push:
    branches:
      - main

jobs:
  SSJProd:
    name : "Sample Job 1"
    runs-on: ubuntu-latest
      step :
        - name : "Sample Step 1"
          shell : bash
          run : echo "Hello from Sample Job 1 - Step 1"